services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: spotifysort
      POSTGRES_USER: spotifysort
      POSTGRES_PASSWORD: spotifysort
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
